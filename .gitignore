# Root-level ignores
.env.local
node_modules/
.DS_Store

# Edge launch configuration (local only; contains local secrets)
edge/.launch.sh
edge/.env.local
edge/launch.sh

# Ignore local Next.js outputs if created at root
.next/
